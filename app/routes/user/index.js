import Ember from 'ember';

export default Ember.Route.extend({
  // model: function(params) {
    // return this.store.find('user', {user_id: params.user_id});
  // },

  // serialize: function(model) {
  //   return { handle: model.get('handle') };
  // }
});
